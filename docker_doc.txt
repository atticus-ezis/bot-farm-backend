docker compose build (initial build)
docker compose up
docker compose ps 

docker compose logs (web)

docker compose exec web python manage.py dbshell

docker - 
    compose build
    compose down 
    compose up 
    compose ps 


curl http://localhost:8000

debugging 

to view env for db 
cd /Users/atticusezis/Desktop/codex_test/backend && 
docker compose exec db env | grep POSTGRES

docker compose run --rm web env | grep POSTGRES_DB
