DOCKER IMAGES:
### for postgresql url use:
atticusezis/botevent-prod:v9amd64
### for locally saved data (sqlite):
use latest

### for local test:
docker build -t codex-test:local .


docker - 
    compose build
    compose down 
    compose up 
    compose ps 


To build and push 

docker buildx build \
  --platform linux/amd64 \
  -t docker.io/atticusezis/botevent-prod:v11amd64 \
  .

docker push docker.io/atticusezis/botevent-prod:v11amd64